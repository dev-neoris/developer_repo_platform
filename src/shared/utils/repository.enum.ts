export enum RepoEnum {
  ENABLED = 'E',
  DISABLED = 'D',
  ARCHIVED = 'A',
}

export enum TribeEnum {
  ERROR_NOT_FOUND = 'La Tribu no se encuentra registrada',
  ERROR_NOT_COVERAGE = 'La Tribu no tiene repositorios que cumplan con la cobertura necesaria',
}
